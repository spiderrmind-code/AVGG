# Identificadores CJ

`supplierOrderId` es el identificador interno genérico. En CJ, `orderId`, `orderNum`, `cjOrderId` y `shipmentOrderId` tienen significados distintos y nunca se sustituyen. `cjOrderId` se usa para información logística; `trackingNumber` sólo se persiste si CJ lo asigna.
