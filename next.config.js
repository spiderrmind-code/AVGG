/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: {
  //   turbo: true, // ← eliminado porque ya no se acepta booleano
  // },
};

module.exports = nextConfig;
